function table(input) {
    let n = Number(input);
    var mytable = "<table border=\"1\"><tr><th>x</th>";

    for (let row = 1; row <= n; row++)
    {
        for (let col = 1; col <= n; col++)
        {

        }
    }
}
        